# Python
__pycache__/
*.py[cod]
*$py.class
*.so
*.egg
*.egg-info/
dist/
build/
.eggs/
.venv/
venv/
env/
.env
pip-wheel-metadata/
*.pth

# Pytest / coverage
.pytest_cache/
.coverage
htmlcov/
coverage.xml

# Distribution
*.whl
*.tar.gz
MANIFEST

# PyInstaller
*.spec
dist/
.pyinstaller/
build/pyinstaller/

# Windows
Thumbs.db
ehthumbs.db
Desktop.ini
$RECYCLE.BIN/
*.lnk

# IDE
.vscode/
.idea/
*.suo
*.user
*.userosscache
*.sln.docview
*.swp
*~

# CIS Benchmark PDFs (proprietary — do not commit)
*.pdf

# Apotrope outputs
*.html
*.json
# brand/ is the palette source of truth — track ALL of its JSON, not just one
# file, so a future brand token (e.g. brand/palette.json) can't silently fall
# back into the *.json ignore above.
!brand/*.json
# docs/ is the published marketing site (GitHub Pages) — commit all of its HTML
!docs/index.html
!docs/report.html
!docs/**/*.html
apotrope_report_*

# Design review scaffolding
gen_sample_report.py
sample_report.html
.gstack/

# Local agent planning scratch (never publish — docs/ is the live site)
.superpowers/
